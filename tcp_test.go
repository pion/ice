package ice

import (
	"testing"
)

func TestPacketTCP(t *testing.T) {
	// aAddr, _ := net.ResolveIPAddr("tcp", "127.0.0.1:10000")
	//	bAddr, _ := net.ResolveIPAddr("tcp", "127.0.0.1:20000")
	// aChan := newPacketTCP(aAddr)
	//	bChan := newPacketTCP(bAddr)

	// x:=test.

}
